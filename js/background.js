// ==================== BACKGROUND ====================
function drawBG(idx){
  const st=STAGES[idx]||STAGES[0];
  const grd=X.createLinearGradient(0,0,0,H);
  switch(st.bg){
    case'volcano':grd.addColorStop(0,'#120300');grd.addColorStop(0.5,'#200800');grd.addColorStop(1,'#100400');X.fillStyle=grd;X.fillRect(0,0,W,H);X.fillStyle='#180500';X.beginPath();X.moveTo(0,H-200);for(let i=0;i<=W;i+=60)X.lineTo(i,H-200-Math.sin(i*0.008)*60-Math.cos(i*0.015)*30);X.lineTo(W,H);X.lineTo(0,H);X.fill();X.fillStyle='#1a0600';X.beginPath();X.moveTo(0,H-140);for(let i=0;i<=W;i+=40)X.lineTo(i,H-140-Math.sin(i*0.012+1)*40);X.lineTo(W,H);X.lineTo(0,H);X.fill();X.fillStyle='#120300';X.beginPath();X.moveTo(680,H);X.lineTo(810,80);X.lineTo(940,H);X.fill();X.fillStyle='rgba(255,60,0,0.35)';X.beginPath();X.arc(810,90,22+Math.sin(Date.now()*0.004)*6,0,Math.PI*2);X.fill();X.fillStyle='rgba(255,80,0,0.06)';X.fillRect(0,GROUND+5,W,H-GROUND);X.strokeStyle='rgba(255,100,0,0.2)';X.lineWidth=2;for(let i=0;i<6;i++){X.beginPath();X.moveTo(i*160+30,GROUND+10);X.quadraticCurveTo(i*160+80,GROUND+12,i*160+60,GROUND+22);X.stroke()}X.fillStyle='rgba(255,150,0,0.4)';for(let i=0;i<12;i++){const ex=(i*89+Date.now()*0.02)%W,ey=GROUND-Math.abs(Math.sin(Date.now()*0.001+i*2))*80;X.beginPath();X.arc(ex,ey,1.5,0,Math.PI*2);X.fill()}drawStars(0.25);break;
    case'ice':grd.addColorStop(0,'#081218');grd.addColorStop(0.5,'#0c1a25');grd.addColorStop(1,'#080e14');X.fillStyle=grd;X.fillRect(0,0,W,H);X.fillStyle='#0a1520';X.beginPath();X.moveTo(0,H-180);for(let i=0;i<=W;i+=50)X.lineTo(i,H-180-Math.sin(i*0.01)*45-Math.cos(i*0.02)*25);X.lineTo(W,H);X.lineTo(0,H);X.fill();drawStars(0.5);X.strokeStyle='rgba(100,200,255,0.15)';X.lineWidth=2;for(let i=0;i<5;i++){const ix=100+i*200,ih=35+Math.sin(i*2.5)*20;X.beginPath();X.moveTo(ix,GROUND);X.lineTo(ix-10,GROUND+ih);X.lineTo(ix+10,GROUND+ih);X.closePath();X.stroke()}X.fillStyle='rgba(200,230,255,0.35)';for(let i=0;i<25;i++){const sx=(i*53+Date.now()*0.015)%W,sy=(i*37+Date.now()*0.012)%H;X.beginPath();X.arc(sx,sy,1,0,Math.PI*2);X.fill()}break;
    case'thunder':grd.addColorStop(0,'#08081a');grd.addColorStop(0.5,'#101030');grd.addColorStop(1,'#080812');X.fillStyle=grd;X.fillRect(0,0,W,H);drawStars(0.6);if(Math.random()<0.015){flashT=3;flashCol='#fff'}X.fillStyle='#101030';for(let i=0;i<4;i++){const px=130+i*240;X.fillRect(px,110,18,GROUND-110);X.fillRect(px-8,100,34,14)}X.fillStyle='rgba(80,80,120,0.15)';for(let i=0;i<4;i++){X.beginPath();X.ellipse(i*280+100,40,120,30,0,0,Math.PI*2);X.fill()}break;
    case'poison':grd.addColorStop(0,'#08120a');grd.addColorStop(0.5,'#0c1a0d');grd.addColorStop(1,'#060e06');X.fillStyle=grd;X.fillRect(0,0,W,H);X.fillStyle='rgba(80,255,40,0.02)';for(let i=0;i<6;i++){const fx=(i*180+Date.now()*0.008)%W;X.beginPath();X.ellipse(fx,GROUND-25,90,35,0,0,Math.PI*2);X.fill()}X.fillStyle='rgba(80,255,40,0.12)';for(let i=0;i<10;i++){const bx=(i*110+Date.now()*0.004*(i+1))%W,by=GROUND+8+Math.sin(Date.now()*0.003+i)*4;X.beginPath();X.arc(bx,by,2.5,0,Math.PI*2);X.fill()}break;
    case'blood':grd.addColorStop(0,'#15050e');grd.addColorStop(0.5,'#200814');grd.addColorStop(1,'#10040c');X.fillStyle=grd;X.fillRect(0,0,W,H);X.fillStyle='rgba(255,0,40,0.2)';for(let i=0;i<8;i++){const ex=60+i*115,ey=90+Math.sin(i*1.8)*35;X.beginPath();X.ellipse(ex,ey,3.5,1.8,0,0,Math.PI*2);X.fill()}break;
    case'void':grd.addColorStop(0,'#040010');grd.addColorStop(0.5,'#08001a');grd.addColorStop(1,'#04000c');X.fillStyle=grd;X.fillRect(0,0,W,H);X.strokeStyle='rgba(120,40,220,0.08)';X.lineWidth=2;for(let i=0;i<5;i++){const a=Date.now()*0.0008+i*1.3;X.beginPath();X.arc(W/2,H/2,80+i*45,a,a+1.2);X.stroke()}X.fillStyle='rgba(120,80,220,0.2)';for(let i=0;i<15;i++){const fx=(i*65+Date.now()*0.008*(i%4+1))%W,fy=(i*40+Math.sin(Date.now()*0.002+i)*25)%H;X.beginPath();X.arc(fx,fy,1.5,0,Math.PI*2);X.fill()}break;
  }
  X.fillStyle='#0e0e0e';X.fillRect(0,GROUND+5,W,H-GROUND);
  X.strokeStyle='#222';X.lineWidth=1;X.beginPath();X.moveTo(0,GROUND+5);X.lineTo(W,GROUND+5);X.stroke();
  X.fillStyle='#161616';for(let i=0;i<W;i+=35){X.fillRect(i,GROUND+8,18,2)}
}
function drawStars(b){X.fillStyle='#fff';for(let i=0;i<35;i++){const sx=(i*141)%W,sy=(i*103)%(H*0.45);X.globalAlpha=b*(0.25+Math.sin(Date.now()*0.0008+i)*0.25);X.beginPath();X.arc(sx,sy,1,0,Math.PI*2);X.fill()}X.globalAlpha=1}
function drawVignette(){if(vignetteAlpha<=0)return;const g=X.createRadialGradient(W/2,H/2,H*0.3,W/2,H/2,H*0.75);g.addColorStop(0,'rgba(0,0,0,0)');g.addColorStop(1,`rgba(0,0,0,${vignetteAlpha})`);X.fillStyle=g;X.fillRect(0,0,W,H)}
function drawComboBorder(){if(comboGlow<=0)return;X.globalAlpha=comboGlow*0.4;X.strokeStyle='#4cf';X.lineWidth=3;X.strokeRect(2,2,W-4,H-4);X.globalAlpha=1}